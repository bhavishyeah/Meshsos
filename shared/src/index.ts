// Shared types barrel export.
// Re-exports every public type and runtime helper from ./types so that
// consumers can import from the package specifier "@meshsos/shared"
// rather than reaching into shared/src with relative paths.
export * from './types/index.js';
